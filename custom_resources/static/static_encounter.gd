class_name StaticEncounterDef
extends EncounterDef

func start_encounter(handler: EnemyHandler) -> void:
	pass
	#var count := RNG.instance.randi_range(min_enemies, max_enemies)
	#handler.spawn_enemies(_pick_species(count), _pick_spawn_points(count))
